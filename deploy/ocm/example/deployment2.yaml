apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: default
  name: mynginx2
  labels:
    cluster.open-cluster-management.io/appbundle: mybundle
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx2
  template:
    metadata:
      labels:
        app: nginx2
    spec:
      containers:
        - name: nginx2
          image: nginx:1.21.5
          ports:
            - containerPort: 80